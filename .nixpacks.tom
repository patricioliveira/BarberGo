[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm turbo run build --filter=@barbergo/hall"]

[start]
cmd = "pnpm --filter @barbergo/hall start -- --port 3006 --hostname 0.0.0.0"
